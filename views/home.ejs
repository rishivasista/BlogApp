<%- include("partials/header") -%>
<h1>Home</h1>
<% posts.forEach((post)=> {%>
    <% let post_content = post.content%>
    <h2><%= post.title%></h2>
    <p> <%= post_content.slice(0, 100)%>.... <a href="/posts/<%= post.title %>">Read More</a></p>
<% }) %>

<%- include("partials/footer") -%>